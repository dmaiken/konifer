api {
  rule-evaluation {
    enabled = true
  }
}

data-store {
  provider = postgresql
  postgresql {
    host = postgres
    port = 5432
    user = konifer_user
    database = konifer
  }
}

object-store {
  provider = s3

  s3 {
    endpoint-url = "http://minio:9000"
    region = "auto"

    # Static credentials
    access-key = "minio_admin"

    force-path-style = true
  }
}

variant-profiles {
  thumbnail-200 {
    w = 256
    r = auto
  }
}

rule-definitions {
  joshua-tree {
    prompts = [
      "a joshua tree",
      "a tree",
      "joshua tree national park"
    ]
    threshold = 0.7
  }
}

paths {
  "/**" {
    object-store {
      bucket = assets
    }
  }
  "/accept/**" {
    upload-ruleset {
      default = reject
      accept-rules = [
        { rule = joshua-tree }
      ]
    }
  }
}
